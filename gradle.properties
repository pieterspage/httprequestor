group=page.pieters
version=0.1.2

org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Publishing a set in ~/.gradle/gradle.properties or via env vars
# ossrhUsername=
# ossrhPassword=
